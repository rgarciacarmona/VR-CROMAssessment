using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class IKMano : MonoBehaviour
{
    //public Transform control;
    //Animator animator;

    //void Start()
    //{
    //    animator = GetComponent<Animator>();
    //}

    //void OnAnimatorIK(int layerIndex)
    //{

    //    Quaternion handRotation = Quaternion.LookRotation(control.position - transform.position);
    //    animator.SetIKRotationWeight(AvatarIKGoal, 1.0f);
    //    animator.SetIKRotation(AvatarIKGoal, handRotation);

   // }
}
